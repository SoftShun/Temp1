---
description: Security review of pending changes on the current branch
---

Invoke the built-in `security-review` skill to review pending changes (staged + unstaged) on the current branch.

If `$ARGUMENTS` is supplied, pass them through as scope hints (e.g. a path or a focus area like "auth only").
